CREATE OR REPLACE FUNCTION DATE_CYMD
        (INDATE  DECIMAL(8, 0) )
        RETURNS DATE
        PARAMETER STYLE SQL
        LANGUAGE RPGLE
        NO SQL
        DETERMINISTIC
        EXTERNAL NAME 'DATE_SQL(DATE_CYMD)'
        RETURNS NULL ON NULL INPUT
        NO EXTERNAL ACTION
;